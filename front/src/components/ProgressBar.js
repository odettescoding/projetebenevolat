// import React, { useState } from 'react';
// import './ProgressBar.css'; // Importez votre fichier CSS pour la stylisation

// const ProgressBar = () => {
//     const [progress, setProgress] = useState(0); // État initial de la progression

//     // Fonction pour mettre à jour la progression
//     const updateProgress = () => {
//       if (progress < maxValue) {
//         setProgress(progress + 1); // Augmente la progression de 1 à chaque fois
//       }
//     };
  
//     return (
//       <div className="progress-bar-container">
//         <div
        
//           className="progress-bar"
//           style={{ width: `${(progress / maxValue) * 100}%` }} // Utilisation de l'attribut de style pour la largeur dynamique
            
//         ></div>
//         <button onClick={updateProgress}>Augmenter la progression</button>
//       </div>
//     );
//   };
  
  

// export default ProgressBar;
